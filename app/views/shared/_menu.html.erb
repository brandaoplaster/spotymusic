<nav class="columns is-mobile is-dark is-fixed-top">

  <div class="column is-2-desktop">
    Logo
  </div>

  <div class="column menu-button">
    <% if user_signed_in? %>
      <div class="dropdown is-hoverable is-pulled-right is-right">
        <div class="dropdown-trigger">
          <a class="icon is-large" aria-haspopup="true" aria-controls="dropdown-menu">
            <i class="fas fa-cog fa-2x has-text-white-bis"></i>
          </a>
        </div>
        <div class="dropdown-menu" id="dropdown-menu" role="dropdown-menu">
          <div class="dropdown-content">
            <%= link_to "Profile", edit_user_registration_patch, class: "dropdown-item" %>
            <hr class="dropdown-divider">
            <%= link_to "Logout", destroy_user_session_patch, class: "dropdown-item" %>
          </div>
        </div>
      </div>
    <% else %>
      <%= link_to "ENTRAR", new_user_session_path, class: "button is-pulled is-outlined is-white" %>
    <% end %>
  </div>

</nav>